#include <iostream>
using namespace std;

int main() {
    system("chcp 1251 > nul"); // ќсигур€ва правилно изобраз€ване на кирилица

    int* динамично„исло = new int; // «адел€не на памет за ц€ло число
    *динамично„исло = 15;
    cout << "—тойност на динамичното число: " << *динамично„исло << endl;

    delete динамично„исло; // ќсвобождаване на паметта
    динамично„исло = nullptr; // ѕрисво€ване на нулев указател за безопасност

    return 0;
}
